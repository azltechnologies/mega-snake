# Presence of this file switches on the opt-in 'snake' project stack of 'mgsnake working-env':
# the launch configuration that debugs the mega-snake CLI itself. It exists in this repository
# only, which is what keeps that configuration out of every other project's workspace.
